#!/usr/bin/env bash
set -e

root_path=$(realpath .)

echo "== SAM deploy..."

sam deploy \
  --template-file "./.aws-sam/build/template.yml" \
  --stack-name "sns-basic-lambda" \
  --capabilities "CAPABILITY_IAM" \
  --region "ca-central-1"